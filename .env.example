# ── Server ───────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development

# ── Database (SQL Server) — chạy trực tiếp (không Docker) ────────
#DB_HOST=160.30.252.5
#DB_USER=api_user
#DB_PASSWORD=Api@123456
#DB_NAME=SUCKHUOETHUDO_DB
#DB_DIALECT=mssql
DB_DIALECT=postgres
DB_HOST=160.30.252.42        # cùng server với aaPanel → localhost; khác server → IP máy chủ
DB_PORT=5432
DB_NAME=suckhuoethudo_db         # tên database đã tạo trên aaPanel
DB_USER=admin       # user đã tạo trên aaPanel
DB_PASSWORD=y276SzZCwhTjeLAb
DB_SSL=false             # đặt true nếu provider yêu cầu SSL (Supabase, Neon, ...)

# ── Docker — thêm các biến này khi chạy docker-compose ───────────
# SA_PASSWORD: tài khoản sysadmin MSSQL (>= 8 ký tự, HOA+thường+số+đặc biệt)
# SA_PASSWORD=SaDockerP@ss2024
# APP_PORT=3000      # cổng expose backend ra host
# MSSQL_PORT=1433    # cổng expose MSSQL ra host

# ── JWT ──────────────────────────────────────────────────────────
JWT_SECRET=836ecad445d331346483e08df0c658295ecd856ab846945ecd9fe3ab1f0bd9e6740e792609a307697b41a04005681a7d38f729264f71293ccc0305cd8bb7fd2c
JWT_EXPIRES_IN=7d

# ── CORS — danh sách origin được phép, ngăn cách bằng dấu phẩy ──
ALLOWED_ORIGINS=https://suckhoethudo.vn,https://admin.suckhoethudo.vn,http://localhost:5173

# ── Frontend URL (dùng cho link trong email) ─────────────────────
FRONTEND_URL=https://suckhoethudo.vn

# ── Email (Nodemailer) ───────────────────────────────────────────
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=your_email@example.com
EMAIL_PASS=your_email_password

# ── Giới hạn ─────────────────────────────────────────────────────
EXPORT_MAX_ROWS=5000
STATS_MAX_FETCH=2000
